# English Words Frontend

Vite + Vue 3 frontend for the English Words backend.

## Install

```powershell
npm install
```

## Run

Start the backend at `http://localhost:8080`, then run:

```powershell
npm run dev
```

Open:

```text
http://localhost:5173
```

The Vite dev server proxies `/api` to the backend.
